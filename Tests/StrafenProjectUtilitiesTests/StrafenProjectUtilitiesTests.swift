import XCTest
@testable import StrafenProjectUtilities

final class StrafenProjectUtilitiesTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(StrafenProjectUtilities().text, "Hello, World!")
    }
}
